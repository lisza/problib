from setuptools import setup

setup(name='problib',
      version='0.2',
      description='Gaussian and Binomial Probability Distributions',
      license='MIT',
      author='lisza',
      packages=['problib'],
      zip_safe=False)